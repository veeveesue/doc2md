# 🚀 快速开始指南

## 3 步快速上手

### 步骤 1: 安装插件

1. 打开 Chrome，访问 `chrome://extensions/`
2. 开启右上角 **"开发者模式"**
3. 点击 **"加载已解压的扩展程序"**
4. 选择 `/path/to/docx-to-md-extension` 文件夹

### 步骤 2: 启动服务器

在终端运行：

```bash
cd /path/to/docx-to-md-extension
./start_server.sh
```

或直接运行：

```bash
python3 /path/to/docx-to-md-extension/server.py
```

### 步骤 3: 开始转换

1. 点击浏览器工具栏的插件图标
2. 确认状态显示 🟢 "服务器已连接"
3. 选择 DOCX 文件或文件夹
4. 点击 "开始转换"

## 💡 使用提示

### 单文件转换
- 点击 "选择 DOCX 文件"
- 可以按住 Ctrl/Cmd 多选文件

### 批量转换
- 点击 "选择文件夹"
- 自动找到文件夹中所有 .docx/.doc 文件

### 图片提取
- 默认开启 ✅
- 图片保存到 `media/` 文件夹
- Markdown 自动引用图片

### 自定义输出路径
- 留空 = 保存到源文件同目录
- 填写路径 = 统一保存到指定位置

## 🎯 常见场景

### 场景 1: 转换单个文件
```
选择文件 → 开始转换 → 完成
```

### 场景 2: 批量转换文件夹
```
选择文件夹 → 开始转换 → 查看进度 → 完成
```

### 场景 3: 转换旧版 .doc 文件
```
1. 安装 LibreOffice
2. 勾选 "自动转换 .doc 文件"
3. 选择文件 → 开始转换
```

## ⚠️ 注意事项

1. **服务器必须运行** - 转换前确保服务器启动
2. **端口不要占用** - 默认使用 8765 端口
3. **文件路径权限** - 确保输出路径有写入权限

## 🆘 遇到问题？

### 服务器无法连接
```bash
# 重启服务器
./start_server.sh
```

### 缺少依赖
```bash
# 安装依赖
pip3 install python-docx
```

### 查看详细文档
阅读 [README.md](README.md) 了解更多详情

---

**现在开始转换吧！** 📝✨
